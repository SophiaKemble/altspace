<%= form_for(@space) do |f| %>
  <% if @space.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@space.errors.count, "error") %> prohibited this space from being saved:</h2>

      <ul>
      <% @space.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :wreck_or_reuse %><br>
    <%= f.text_field :wreck_or_reuse %>
  </div>
  <div class="field">
    <%= f.label :fee_or_free %><br>
    <%= f.text_field :fee_or_free %>
  </div>
  <div class="field">
    <%= f.label :practice_or_perform %><br>
    <%= f.text_field :practice_or_perform %>
  </div>
  <div class="field">
    <%= f.label :sq_footage %><br>
    <%= f.number_field :sq_footage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
